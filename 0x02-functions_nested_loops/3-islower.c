#include "main.h"

/**
 * int_islower(int c) - checks for lowercase characters
 * Return: 1 if c is lowercase, 0 if it's not
 */

int_islower(int c)
{
	char c;
	if (c = 'a'; c <= 'z';)
	{
		return (1);
	}
	else (c = 'A'; c <= 'Z';)
	{
		return (0);
	}
}
