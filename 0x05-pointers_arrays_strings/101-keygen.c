#include <stdio.h>
#include "main.h"
#include <time.h>

/**
 * main - prints a program that generates valid passwords for 101-crackme
 * Return: 0
 */

int main(void)
{
	char password[84];
	int index = 0; sum = 0; half_diff1; half_diff2;

	srand(time(0));
}
