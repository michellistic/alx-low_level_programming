#include <stdlib.h>

#include <time.h>

#include <stdio.h>

/** 
 * main - Prints if number is positive, zero, or negative
 * Return: Always (success)
 *
 */

int main(void)
{
	int m;

	srand(time(0));
	n = rand(
